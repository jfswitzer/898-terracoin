import miner
import validator
import terra-engine

if __name__ == '__main__':
    #spawn all threads
    tasks = [terra-engine.transaction_generator,terra-engine.check_validated, miner.mine, validator.validate]
    for task in tasks:
        t = threading.Thread(target=task)
        t.start()
